import React from "react";
import BasicLayout from "../layouts/BasicLayout";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <BasicLayout>
        <p></p>
      </BasicLayout>
    </div>
  );
};

export default Home;
